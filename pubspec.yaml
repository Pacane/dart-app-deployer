name: poker_planning_webhook
version: 0.0.1
description: A daemon that listens to GitHub push webhook and that auto-redeploys the app.
dependencies:
  crypto: '>=0.9.0 <0.10.0'
  dart_config: any
  mock: ">=0.11.0+2 <0.12.0"
